# --- Local development ---
# The Vite dev server proxies /api and /uploads to this origin.
VITE_API_PROXY="http://localhost:4000"

# --- Production (Cloudflare Pages) ---
# Full URL of the deployed API. Leave empty for local dev (the proxy handles it).
# Example: https://cargo-admin-api.onrender.com
VITE_API_BASE_URL=""
